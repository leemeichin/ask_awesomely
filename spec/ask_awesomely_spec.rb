require 'spec_helper'

describe AskAwesomely do
  it 'has a version number' do
    expect(AskAwesomely::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
